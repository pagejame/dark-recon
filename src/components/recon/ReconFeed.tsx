import Card from '@/components/ui/Card';

export default function ReconFeed() {
  return (
    <Card>
      <h2 className="font-heading text-lg font-semibold text-text-primary">Recon Feed</h2>
      <p className="mt-4 text-sm text-text-secondary">
        Recon agents are scanning markets. Live feed will appear here.
      </p>
    </Card>
  );
}
