export async function assessRisk(ticker: string, positionSize: number) {
  // Placeholder: evaluate position sizing and portfolio risk
  return { ticker, positionSize, approved: false, maxSize: 0 };
}
